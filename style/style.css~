body{
	margin:0;
}

[data-type="list"] header, p, li {
	margin-left: 4%;
	margin-right: 4%;
    font-size: 0.8rem;
}

[data-type="list"] .description_date {
    line-height: 1rem;
    font-weight: 400;
}

a {
	text-decoration: none;
	color: #000;
}

/*Color Adjust*/

.borderBlue{
	border-bottom: 0.1rem solid #2574a9;
}

.colorBlue{
	color: #2574a9;
	background: #2574a9;
	background-color: #2574a9;
}

[data-type="list"] header {
  border-bottom: 0.1rem solid #2574a9;
  padding: 0 1.5rem;
  margin: 0;
  font-size: 1.2rem;
  height: 1.5rem;
  line-height: 1.5rem;
  font-weight: normal;
  color: #2574a9;
  text-transform: uppercase;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

#logo {
    width: 55%;
    padding: 0.5rem 0.9rem;
}

#header {
    z-index: 10;
}

menu[type="toolbar"] button {
    height: 80%;
    width: 80%;
}

[data-type="list"] {
    text-align: center;
}

section > header {
  border-bottom: 0.1rem solid #2574a9;
  padding: 0 1.5rem;
  margin: 0;
  font-size: 1.2rem;
  height: 1.5rem;
  line-height: 1.5rem;
  font-weight: normal;
  color: #2574a9;
  text-transform: uppercase;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.sections {
    text-align: center;
    padding: 3.5rem 0;
    z-index: 0;
}

.sections > p {
    font-size: 1rem;
}

.link {
	color:#3498db;
	text-decoration: underline;
}

.hidden{
    display:none;
}

.show{
    display:static;
}
